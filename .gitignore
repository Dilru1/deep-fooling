1.  **Initialize Git:**
    ```bash
    git init
    ```
2.  **Add Files:**
    ```bash
    git add .
    ```
3.  **Commit:**
    ```bash
    git commit -m "Initial commit: RL sailboat training with PPO and custom environments"
    ```
4.  **Connect to GitHub:**
    (Create a new empty repo on GitHub first)
    ```bash
    git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
    git branch -M main
    git push -u origin main
    ```

model/